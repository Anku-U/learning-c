// program to calculate simple interest for given input
#include<stdio.h>
#include<conio.h>
int main() {
    int p = 100, r = 5, t =1;
    float si = (p*r*t)/100;
    printf("simple interest for given input is %f", si);
    return 0;
}